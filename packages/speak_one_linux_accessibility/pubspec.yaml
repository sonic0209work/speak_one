name: speak_one_linux_accessibility
description: Dart FFI plugin for system-wide text selection detection via AT-SPI2 (Wayland/GNOME) and XCB (X11) on Linux.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.10.0

dependencies:
  ffi: ^2.1.3

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
